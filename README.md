# This is my html template

The HTML is in the `public` folder.

It uses OpenProps for styling, datastar for interaction
and Google Fonts.

The Go code provides an `embedded` html server that
includes the HTML, css and javascript.
